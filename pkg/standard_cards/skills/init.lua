local prefix = "packages.hegemony.pkg.standard_cards.skills."

return {
  require(prefix .. "befriend_attacking"),
  require(prefix .. "known_both"),
  require(prefix .. "await_exhausted"),
  require(prefix .. "hegNullification"),
  require(prefix .. "six_swords"),
  require(prefix .. "triblade"),
}
